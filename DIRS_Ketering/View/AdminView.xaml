<UserControl x:Class="DIRS_Ketering.View.AdminView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DIRS_Ketering.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <TabControl>
        <TabItem Header="Jela">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="250"/>
                </Grid.ColumnDefinitions>
                <DataGrid Grid.Column="0"
                          ItemsSource="{Binding Jela}"
                          SelectedItem="{Binding SelektovanoJelo}"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          Margin="10">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Naziv" Binding="{Binding Naziv}" Width="*"/>
                        <DataGridTextColumn Header="Opis" Binding="{Binding Opis}" Width="*"/>
                        <DataGridTextColumn Header="Cena" Binding="{Binding Cena}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>
                <StackPanel Grid.Column="1" Margin="10">
                    <TextBlock Text="Naziv"/>
                    <TextBox Text="{Binding Naziv, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                    <TextBlock Text="Opis"/>
                    <TextBox Text="{Binding Opis, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,10"/>

                    <TextBlock Text="Cena"/>
                    <TextBox Text="{Binding Cena, UpdateSourceTrigger=PropertyChanged}" Margin="0,0,0,20"/>

                    <Button Content="Dodaj" Command="{Binding NovoJeloCommand}" Margin="0,0,0,5"/>
                    <Button Content="Azuriraj" Command="{Binding AzurirajJeloCommand}" Margin="0,0,0,5"/>
                    <Button Content="Obrisi" Command="{Binding ObrisiJeloCommand}" Margin="0,0,0,5"/>
                    <Button Content="Ocisti" Command="{Binding OcistiFormuCommand}"/>

                </StackPanel>

            </Grid>
        </TabItem>
        <TabItem Header="Prikaz rezervacija">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="300"/>
                </Grid.ColumnDefinitions>
                <DataGrid Grid.Column="0"
              ItemsSource="{Binding Porudzbine}"
              SelectedItem="{Binding SelektovanaPorudzbina}"
              AutoGenerateColumns="False"
              IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                        <DataGridTextColumn Header="Datum kreiranja" Binding="{Binding VremeKreiranja, StringFormat='{}{0:dd.MM.yyyy}'}" Width="130"/>
                        <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="100"/>
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Grid.Column="1" Margin="10">
                    <TextBlock Text="Detalji rezervacije" FontWeight="Bold" Margin="0,0,0,10"/>

                    <TextBlock Text="Datum isporuke"/>
                    <TextBlock Text="{Binding SelektovanaPorudzbina.DatumIsporuke, StringFormat='{}{0:dd.MM.yyyy}'}" Margin="0,0,0,10"/>

                    <TextBlock Text="Adresa isporuke"/>
                    <TextBlock Text="{Binding SelektovanaPorudzbina.AdresaIsporuke}" Margin="0,0,0,10"/>

                    <TextBlock Text="Status"/>
                    <ComboBox ItemsSource="{Binding Statusi}" SelectedItem="{Binding SelektovaniStatus}" Margin="0,0,0,20"/>

                    <TextBlock Text="Stavke" FontWeight="Bold" Margin="0,0,0,5"/>
                    <ScrollViewer Height="80" VerticalScrollBarVisibility="Auto">
                        <ListBox ItemsSource="{Binding SelektovanaPorudzbina.Stavke}">
                            <ListBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="{Binding Jelo.Naziv}" Margin="0,0,10,0"/>
                                        <TextBlock Text="{Binding Kolicina, StringFormat='x{0}'}"/>
                                        <TextBlock Text="{Binding TrenutnaCena, StringFormat=' - {0} RSD'}"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ListBox.ItemTemplate>
                        </ListBox>
                    </ScrollViewer>
                    <Button Content="Azuriraj" Command="{Binding PromeniStatusCommand}" Margin="0,20,0,0"/>
                </StackPanel>
            </Grid>
        </TabItem>
        <TabItem Header="Pomocni panel">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="180"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <Button Content="Izvezi kao JSON" Command="{Binding ExportDb}" Width="150" Height="20" Margin="0,100,0,20"></Button>
                    <Button Content="Generisi PDF izvestaj" Command="{Binding ExportPDFCommand}" Width="150" Height="20"></Button>
                </StackPanel>
                <StackPanel Grid.Column="1" Background="LightGray">
                    <TextBlock HorizontalAlignment="Center" FontSize="18" Text="Dobrodosli na administratorski panel!" FontWeight="Bold" Margin="0,0,0,60"></TextBlock>
                    <TextBlock HorizontalAlignment="Center" FontSize="14" Text="Panel se sastoji od tri taba: Jela, Prikaz rezervacija i Pomocni panel"></TextBlock>
                    <TextBlock HorizontalAlignment="Center" FontSize="14" Text="Na panelu Jela mozete vrsiti upravljanje aktuelnim jelovnikom"></TextBlock>
                    <TextBlock HorizontalAlignment="Center" FontSize="14" Text="Na panelu prikaz rezervacija mozete videti sve rezervacije i po potrebi menjati njihov status"></TextBlock>
                    <TextBlock HorizontalAlignment="Center" FontSize="14" Text="Na pomocnom panelu mozete odabrati polja za generisanje PDF izvestaja ili izvoz JSON datoteke"></TextBlock>
                </StackPanel>
            </Grid>
        </TabItem>
    </TabControl>
</UserControl>
